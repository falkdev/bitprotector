name: Setup Frontend
description: Install Node.js 20.19 and restore npm cache

runs:
  using: composite
  steps:
    - name: Setup Node.js 20.19
      uses: actions/setup-node@v4
      with:
        node-version: '20.19'
        cache: npm
        cache-dependency-path: frontend/package-lock.json
