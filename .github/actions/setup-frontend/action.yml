name: Setup Frontend
description: Install Node.js 24 and restore npm cache

runs:
  using: composite
  steps:
    - name: Setup Node.js 24
      uses: actions/setup-node@v5
      with:
        node-version: '24'
        cache: npm
        cache-dependency-path: frontend/package-lock.json
